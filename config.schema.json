{
  "pluginAlias": "StaggKettle",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Homebridge plugin for Fellow Stagg kettles. See the [README](https://github.com/Willmac16/homebridge-kettle#readme) for setup instructions.",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "title": "Name",
        "default": "Kettles"
      },
      "kettles": {
        "type": "array",
        "title": "Kettles",
        "items": {
          "type": "object",
          "title": "Kettle",
          "properties": {
            "name": {
              "type": "string",
              "title": "Name",
              "description": "Accessory name shown in HomeKit.",
              "default": "Kettle",
              "minLength": 1
            },
            "connection": {
              "type": "string",
              "title": "Connection Type",
              "oneOf": [
                { "title": "Wi-Fi CLI (EKG Pro)", "enum": ["wifi"] },
                { "title": "BLE bridge (EKG+ / EKG Pro)", "enum": ["ble"] }
              ],
              "default": "wifi"
            },
            "url": {
              "type": "string",
              "title": "Base URL",
              "description": "Wi-Fi: base URL of the kettle (no trailing slash). BLE: base URL of the bridge.",
              "default": "http://192.168.1.32",
              "pattern": "^https?://"
            },
            "minTemp": {
              "type": "number",
              "title": "Minimum Temperature (°C)",
              "description": "Lowest target temperature exposed to HomeKit.",
              "default": 40,
              "minimum": 0,
              "maximum": 100
            },
            "maxTemp": {
              "type": "number",
              "title": "Maximum Temperature (°C)",
              "description": "Highest target temperature exposed to HomeKit.",
              "default": 100,
              "minimum": 0,
              "maximum": 100
            },
            "pollIntervalHeating": {
              "type": "number",
              "title": "Heating Poll Interval (ms)",
              "description": "How often to check kettle state and publish temperature while heating.",
              "default": 10000,
              "minimum": 1000
            },
            "pollIntervalIdle": {
              "type": "number",
              "title": "Idle Temperature Interval (ms)",
              "description": "How often to publish temperature while idle. State is still checked at the heating interval.",
              "default": 1800000,
              "minimum": 1000
            }
          },
          "required": ["name", "connection", "url"]
        }
      }
    }
  }
}
